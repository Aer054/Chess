<Window x:Class="Chess.DrawWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chess"
        mc:Ignorable="d"
        Title="DrawWindow" Height="120" Width="220" ResizeMode="NoResize">
    <Grid>
        <Border>
            <StackPanel>
                <DockPanel Height="82">
                    <Button Content="Соглситься" HorizontalAlignment="Left" Margin="20,42,0,0" VerticalAlignment="Top" Command="{Binding Accept}"/>
                    <Button Content="Отказать" HorizontalAlignment="Left" Margin="30,42,0,0" VerticalAlignment="Top" Width="69"  Command="{Binding Cancel}"/>
                    <Label Content="Соперник предложил ничью" HorizontalAlignment="Center" Margin="-180,10,0,0" VerticalAlignment="Top"/>
                </DockPanel>
            </StackPanel>
            </Border>
    </Grid>
</Window>
